<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.xy.blog.system.mapper.BlogUserRoleMapper">

    <select id="selectRoleKeysByUserId" resultType="java.lang.String">
        SELECT DISTINCT role.role_key
        FROM blog_user_role user_role
        INNER JOIN blog_role role ON role.role_id = user_role.role_id
        WHERE user_role.user_id = #{userId}
          AND role.status = '1'
          AND role.del_flag = '0'
    </select>

</mapper>
