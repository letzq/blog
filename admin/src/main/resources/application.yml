server:
  port: 8080

spring:
  application:
    name: blog
  profiles:
    active: local

springdoc:
  api-docs:
    enabled: true
    path: /v3/api-docs
  swagger-ui:
    enabled: true
    path: /swagger-ui.html
