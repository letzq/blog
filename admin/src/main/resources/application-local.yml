spring:
  config:
    import: optional:classpath:mail-local.yml
  datasource:
    type: com.alibaba.druid.pool.DruidDataSource
    url: jdbc:mysql://localhost:3306/blogs?useUnicode=true&characterEncoding=utf-8&serverTimezone=Asia/Shanghai
    username: root
    password: love2002
    driver-class-name: com.mysql.cj.jdbc.Driver
    druid:
      initial-size: 5
      min-idle: 5
      max-active: 20
      max-wait: 60000
      time-between-eviction-runs-millis: 60000
      min-evictable-idle-time-millis: 300000
      validation-query: SELECT 1
      test-while-idle: true
      test-on-borrow: false
      test-on-return: false
      pool-prepared-statements: true
      max-pool-prepared-statement-per-connection-size: 20
  data:
    redis:
      host: localhost
      port: 6379
      database: 0
      password: love2002
      timeout: 10s
      lettuce:
        pool:
          max-active: 8
          max-idle: 8
          min-idle: 0
          max-wait: -1ms
  security:
    user:
      name: admin
      password: love2002

token:
  secret: blog-ymw@121617870433516jwt-secret
  expire-time: 360
  header: Authorization
  prefix: "Bearer "

logging:
  level:
    root: info
    org.springframework.web: info
    org.springframework.security: info
    com.xy.blog: debug
